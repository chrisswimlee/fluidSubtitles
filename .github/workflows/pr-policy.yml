name: PR Policy

on:
  pull_request_target:
    types: [opened, edited, synchronize, reopened, ready_for_review]
  schedule:
    - cron: "17 8 * * *"
  workflow_dispatch:

permissions:
  contents: read
  issues: write
  pull-requests: write

jobs:
  pr-policy:
    name: PR Policy
    runs-on: ubuntu-latest
    steps:
      - name: Checkout policy from base branch
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          ref: ${{ github.event.repository.default_branch }}
          persist-credentials: false

      - name: Set up Node.js
        uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4.4.0
        with:
          node-version: "20"

      - name: Unit-test PR policy
        run: node --test .github/policy/github-policy.test.mjs

      - name: Validate PR template and visual evidence
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: node .github/policy/enforce-pr-policy.mjs
