# Watch proof note (archived)

Watch is not a product gate. Theater Listen is Voice or Translate on the microphone. Keep [STAGE_SCORE.md](../STAGE_SCORE.md) as the only talk proof. Do not invent numbers.

The leftover capture path (`SCStream`, Screen Recording) may still exist in engine files. Do not treat this note as first-hour product.

Hosted CI cannot caption a live tab.

## Leftover setup (not first hour)

- macOS 26, Apple Silicon
- Leftover capture: **This Mac** (not Safari or Chrome)
- Voice Engine: Apple Speech Analyzer (enough to try) or the engine that hears the video language
- I speak / Show as: a Korean, English, Thai, or Japanese pair

## Leftover steps

1. Grant Screen Recording if needed, then quit and reopen FluidSubtitles.
2. If a leftover Watch picker is still on a branch, do not restore it as product.
3. Play one Korean, English, Thai, or Japanese YouTube (or another non-DRM app) only if you are debugging leftover capture.
4. Score a lectern Listen with [STAGE_SCORE.md](../STAGE_SCORE.md) instead.

Date:

macOS / chip:

Voice Engine:

Video (title or URL, language):

First caption said:

Listen clock (`mic · e2e · ASR · MT`):

Anything that stayed silent (DRM, a call, a helper mix, a virtual/aggregate loop):

Do not invent that result.
